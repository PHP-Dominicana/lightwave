APP_ENV=development
VITE_SERVER_PORT=5173

# Database configuration:
# - Use DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, and DB_NAME for MySQL or similar relational databases.
# - Use DB_FILE only when using SQLite instead (provide the full path to the SQLite file).

DB_HOST=
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=


DB_FILE=